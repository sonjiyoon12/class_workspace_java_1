package ch06;

// 1. 클래스를 설계하는 측
public class Warrior {

    // 멤버 변수
    // 속성(상태) 설계
    // 인스턴스화(메모리에 올리다) 될 때
    // 기본 값(0)으로 초기화 된다.
    String name;
    int hp;
    int power;
    int dp;
    String color;
    double weight;
    boolean isDie;


}
